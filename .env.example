# No environment variables are required for the project shell.
#
# Future server-only provider credentials will be documented here. Never prefix
# secrets with NEXT_PUBLIC_ because those variables are exposed to the browser.
